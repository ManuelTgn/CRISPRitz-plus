from setuptools import setup

# NOTE:
# This project builds a C++ extension via CMake using scikit-build-core.
# Use:
#   pip install -e .
# or:
#   pip wheel .
#
# setup.py is kept only for legacy tooling.

setup()
